base: configs/phase2_braid.yaml

training:
  matte_gate: false
  resume_from: checkpoints/exp3_phase1/final.pth
  batch_size: 16
  gradient_accumulation_steps: 1

checkpointing:
  output_dir: checkpoints/exp3_phase2/
  save_every: 10
