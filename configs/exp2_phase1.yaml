base: configs/phase1_unbraid.yaml

model:
  zero_matte_cond: true

training:
  matte_gate: true
  batch_size: 16
  gradient_accumulation_steps: 1

checkpointing:
  output_dir: checkpoints/exp2_phase1/
  save_every: 10
