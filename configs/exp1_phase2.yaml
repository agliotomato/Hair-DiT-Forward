base: configs/phase2_braid.yaml

training:
  matte_gate: true
  resume_from: checkpoints/exp1_phase1/final.pth
  batch_size: 16
  gradient_accumulation_steps: 1

checkpointing:
  output_dir: checkpoints/exp1_phase2/
  save_every: 10
