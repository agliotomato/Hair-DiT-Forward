# Back-12 ControlNet — Phase 2 Braid Finetune
# ctrl_cond = cat([sketch_lat, matte_raw], dim=1)  (17ch, raw matte only, no MatteCNN)

base: configs/phase2_braid.yaml

model:
  block_offset: 12
  zero_matte_feat: true

training:
  schedule: none
  resume_from: checkpoints/back12_raw_phase1/epoch_40.pth
  batch_size: 16

checkpointing:
  output_dir: checkpoints/back12_raw_phase2/

wandb:
  tags: [back12, back12_raw, phase2, back-block, raw-only, no-gate]
