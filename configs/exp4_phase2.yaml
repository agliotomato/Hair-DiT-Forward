base: configs/phase2_braid.yaml

model:
  zero_matte_cond: true

training:
  matte_gate: false
  resume_from: checkpoints/exp4_phase1/final.pth

checkpointing:
  output_dir: checkpoints/exp4_phase2/
  save_every: 10
