# Final confirmed architecture — Phase 1 (unbraid pretraining)
#
# ctrl_cond: cat(sketch_latent[16ch], matte_bilinear[1ch]) = 17ch
# No MatteCNN. ControlNet blocks initialized from even transformer blocks (0,2,4,...,22).
# Injection: stride=2, residual[i] → transformer blocks 2i and 2i+1.

base: configs/phase1_unbraid.yaml

checkpointing:
  output_dir: checkpoints/final_arch_phase1/
