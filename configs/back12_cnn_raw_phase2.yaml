# Back-12 ControlNet — Phase 2 Braid Finetune
# ctrl_cond = cat([sketch_lat + matte_feat, matte_raw], dim=1)  (17ch, MatteCNN + raw)

base: configs/phase2_braid.yaml

model:
  block_offset: 12

training:
  schedule: none
  resume_from: checkpoints/back12_cnn_raw_phase1/epoch_40.pth
  batch_size: 16

checkpointing:
  output_dir: checkpoints/back12_cnn_raw_phase2/

wandb:
  tags: [back12, back12_cnn_raw, phase2, back-block, cnn+raw, no-gate]
